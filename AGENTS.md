# Repository operation boundaries

## Owner release and next-campaign direction — 22 September 2026

The owner explicitly authorized committing, merging and releasing the confirmed
Windows-tested candidate as 0.3.0. This supersedes older no-merge/push/tag/publication
notices for this release only. Publish the unchanged qualified runtime and optional
Windows quality helper with source/notices, preserving separate private/public
histories. Windows Steam/GOG qualification is complete within recorded workloads;
Linux/Deck remains unqualified. This release authorization accepts that disclosed
scope, not a new Linux test claim or permission to access the Deck.

The owner then explicitly required the mod description remain unchanged. Do not
change release/About.xml description or the Workshop description, preview or
unrelated listing fields; put changes and qualification limits in release notes.
The next steward is to pursue early whole-stage XML reuse, direct single-pass image
decoding and overlapping independent loading work. On-demand loading is explicitly
excluded. Prepare its handoff; do not begin that campaign during publication.
Performance windows are closed; no new live runs are needed for unchanged binaries.
The old campaign timer stays paused. Future publication needs a fresh authorization.


## Owner Windows Steam overnight qualification — 22 September 2026

At approximately 2026-09-22 06:33 UTC the owner left the PC overnight and authorized
Windows Steam qualification plus necessary retained-candidate work toward release
readiness. This supersedes the prior stop-before-Steam and closed-window notices.
The approval includes the prepared single-fixture qualification plan, its narrow
hash-pinned admission update, necessary correctness fixes/builds, fixture UI checks
using the Windows control skill, and bounded performance comparisons after correctness.
No new optimization pilot, retired path or deferred C10/C11/companion is restored.

As a conservative operating limit, finish measurements by 2026-09-22 14:00 UTC
(10 AM Toronto), earlier on owner return or revocation. The owner supplied no exact
cutoff; this is a steward limit. Do not start a sample unlikely to finish by then.
Never close normal games; all must already be stopped for performance. Preserve
other applications and exclude contested samples. Functional runs use existing
fixture-specific admission. Keep one operating owner and the single transactional
fixture, normal installations/data read-only, and restore the GOG baseline afterward.

Windows Steam approval is not publication approval. Linux/Deck remains its separate
later platform decision; do all useful authorized Windows work before raising it.
Do not merge, push, tag or publish. Prepare a brief plain-language comparison against
the last actual version-tagged release, verified locally, when qualification closes.
Resume the steward timer, review actual changes/evidence, and stop only on completed
authorized readiness or a concrete human-only blocker, not merely a subtask handoff.

## Owner atlas retirement and return to PC — 21 September 2026

The owner approved retiring static atlas caching and atlas batching, and requested
cleanup before stopping ahead of Windows Steam qualification. Remove their release
activation and optimization requirements; use native atlas behavior. Preserve shared
foundations needed by the retained loading display and keep research/history. This
is not approval to remove the display or other retained features.

The owner is using the PC again: the overnight performance and fixture desktop-control
window has ended. No performance measurements, microbenchmarks or desktop automation.
Standing isolated GOG correctness permission remains, with the established fixture-
specific process guard and incidental-focus allowance; use only focused checks needed
for this cleanup. No Steam qualification or admission update, Deck, normal-data edits,
merge, push or publication. Refresh the private candidate, independently review it,
then stop and pause the steward timer before Steam. No new optimization pilot.

## Owner overnight closeout authorization — 21 September 2026

At 2026-09-21 07:06 UTC (03:06 Toronto), the owner left the PC overnight and
asked for autonomous testing, fixes and iteration toward a release-ready retained
candidate by roughly 10–11 AM. Performance testing and Windows control skill UI
interaction are explicitly authorized for this window. The proposed new PNG
optimization is explicitly excluded; C10/C11/companion and the six retired paths
remain deferred/retired. This notice supersedes older no-performance, no-desktop-
automation and owner-only visual-inspection limits for the authorized fixture work.
Record agent-observed UI evidence honestly, not as owner inspection.

Use 2026-09-21 14:00 UTC (10 AM Toronto) as a conservative measurement cutoff,
ending earlier on owner return/revocation. Do not start a sample that cannot finish
before cutoff. The unrelated project may continue; do not control its applications.
Exclude resource-contested measurements, pause our competing builds/scans during
measurement, and preserve all game/process guards. Never close a normal game to
obtain idle measurements. Use the single fixture and serial ownership; keep normal
data untouched. Separate platform-promotion and merge/push/publication approvals
remain unchanged; complete all useful currently authorized GOG work before surfacing
any remaining platform decision. Routine fixture UI checks no longer need owner
participation. Resume the five-minute steward timer and avoid needless process work.

**Owner-approved foreground exception, 19 September 2026:** proceed with isolated
GOG fixture correctness tests while the owner uses the PC. The owner explicitly
accepts incidental fixture focus and will put it back in the background. This
resolves the minimized-launch coordination block for the functional batch. Do not
manipulate desktop focus or other applications. No performance measurements,
owner-assisted visual acceptance, Steam/Deck promotion or publication follows.

**Owner-approved scope amendment, 19 September 2026:** retire release activation
and optimization requirements for C01 parsed XML, C02 inheritance reuse, C03 ordered
read-ahead, C04 parsed-language persistence, C05 faithful authored-DDS recaching and
current C06 managed first-build acceleration. Use native behavior; preserve research
and reusable foundations. Preserve separately useful processed XML, translation
application, PNG warm reuse and explicit C08 quality conversion. C15A cleanup and
combined GOG correctness may precede remaining C14 acceptance; do not waive actual
C08/C14 visible/focus obligations. C10/C11/companion stay deferred. No performance
window or additional platform/publication authority follows. Latest ledger/plan apply.


**Latest owner resumption, 19 September 2026:** resume the actual checkout and
latest handoffs; prioritize a useful, stable retained baseline and combined GOG
correctness. All earlier unattended windows are closed. No performance runs,
microbenchmarks or cost trials without a fresh explicit unattended grant. Keep
C10 on-demand assets, C11 progressive detail and companion app deferred. Audit
existing measured benefit/cost and a concrete changed premise before another
optimization attempt; propose a specific scope amendment for persistent losers.
A slower implementation is not a beneficial conservative fallback. After baseline
consolidation propose one measured-bottleneck, supplier-informed aggressive pilot:
aggressive where supported, otherwise validated beneficial conservative behavior,
otherwise native, with the smallest safe fallback unit and inspection costs.
Prove that pilot before general tier-selection infrastructure. Supplier safety
and compatibility-check overhead explanations remain hypotheses to verify.
Standing isolated GOG correctness permission continues; no new Steam/Deck,
desktop automation, normal-data change, merge, push or publication authorization.
The dated notices below retain their historical scope; this instruction governs.

**Current owner unattended window, 13 September 2026 04:49:27 UTC:** the owner
stated the PC will be unattended for a while and authorized any needed performance
tests. This supersedes the prior owner-returned prohibition while this window is
active; no fixed cutoff was supplied. End it on owner return or revocation. Finish
necessary GOG correctness before measuring the affected behavior, use serial
measurement ownership and matched controls, and pause competing builds/substantial
local work during measurements. All RimWorld instances must already be stopped;
never close normal applications to arrange that. Incidental fixture foreground
during this unattended window no longer interrupts the owner, so the C14 launch
coordination pause is lifted for automated native checks. No desktop automation
or claimed owner-assisted visual inspection follows. C10/C11 remain deferred.
No Steam/Deck promotion, normal-data changes, merge, push or publication is granted.

**Owner scope change, 13 September 2026 UTC:** defer on-demand asset development
(C10 / matrix 5b) and progressive texture detail (C11 / 5c), alongside the already
optional companion app (6c). Preserve work/history without claiming delivery or
adoption. Do not resume C10/C11 development or measurements as a release gate.
Continue C14 loading-only background behavior, then C15 retained-feature cleanup
and combined GOG correctness. Retire deferred-feature activation and misleading
claims from the candidate while preserving reusable foundations and native loading.
C14 no longer depends on C10; use native readiness. All retained-feature coverage,
correctness and performance criteria remain. Target the point where only unattended
performance and separately approved Steam then Deck qualification remain. This is
no performance, platform, UI, merge or publication grant. Current plan supersedes
the earlier C10/C11-required and sequencing notices below.

**Owner returned, 12 September 2026:** the owner ended the unattended window.
No performance-oriented tests, microbenchmarks, cost-attribution trials or
performance experiments until a new explicit grant. Continue implementation and
isolated GOG functionality/correctness checks. Functional timings are diagnostic
only. This notice supersedes the historical window below. Current ownership and
next work are in the execution ledger and ignored steward state.

**Historical owner unattended window, effective 12 September 2026 17:35:27 UTC:**
The owner is away for several hours and permits outstanding performance tests
until they say otherwise. This new grant supersedes prior no-window notices only
while active; it ends on owner return or revocation, with no fixed cutoff given.
Finish the current C10 public-patch correctness increment before bounded natural
audio measurements. Preserve all measurement isolation, matched controls, serial
ownership and platform limits. No production adoption or acceptance is implied.

**Owner-approved resequencing, 12 September UTC:** proceed with C12 then C13,
return to the existing C10 orchestrator for completion, then C11, C14 and C15.
This approved amendment to the proposal at `5b8bfaf` permits C12 despite C10's
missing functional acceptance. It does not reduce scope: C10/C11 remain release
requirements. Read the active plan and execution ledger; preserve serial ownership
and independent functional review for every handoff. Performance remains prohibited
until a new explicit unattended window.

**Current owner instruction, 10 September — approved implementation:** the owner approved the reviewed replacement plan at `f70aa08` and directed the steward to proceed with fresh serial orchestrator tasks, independent review and corrections to the same owner. Use the [execution ledger](docs/ecosystem-replacement/execution-ledger.md) for active scope. Preserve prior implementation ancestry; do not merge or publish. Approval does not waive the operating limits below.

**Later owner sequencing instruction, 10 September:** continue through the implementation slices after independent review of feature coverage, correctness, compatibility and expected behavior. Defer performance testing until the owner can provide an explicit unattended window. Performance pending alone must not block the next slice or pause the steward timer while useful implementation remains. Record functional review separately from full acceptance; measured performance still gates full acceptance and release readiness. Preserve exact feature commits/settings and pending measurement obligations for later individual and combined tests. Keep one editing/operating team at a time; corrections return to the responsible orchestrator after ownership is transferred.

Standing permission now covers GOG functionality/correctness tests and necessary modlist variants inside the single isolated fixture, including builds, preparation and deployment. Unless the owner overrides this to the steward, repeat approval is not needed for covered tests. **Performance testing is prohibited while the PC is in use or unless the owner explicitly declares an unattended window.** This includes microbenchmarks and performance experiments. Record the window/limits and pass them, together with any override, to every orchestrator. Functional timings are diagnostic only. Old overnight grants and silence do not create a new window. See [standing testing permission](docs/development.md#standing-campaign-testing-permission).

Preserve normal game/data and other applications. Do not steal focus, automate desktop interaction, access the Deck or publish. Visible/audible/focus checks that would interrupt the owner need coordinated inspection. C01 restored the fixture to verified GOG generation gog-rev573-20260910-194017-cf1a1eb0 through transaction 20260910-194136-685716c0. Matching GOG core/observer builds are required before live work.

Develop and validate all slices and the combined candidate on GOG first. Promote the combined release candidate to the isolated Windows Steam generation only with the owner's explicit approval. Linux/Steam Deck is a later separately authorized promotion after Windows Steam behavior is proven. Keep normal installations untouched; the single fixture changes generations transactionally. The unattended-only performance rule applies throughout.

Read [current state](docs/current-state.md) before project work and [development](docs/development.md) before building or operating the fixture. Read the [ecosystem-replacement plan](docs/ecosystem-replacement/next-campaign-proposal.md) for active slice scope and ownership, and [archived results](docs/archive/results.md) before revisiting experiments. Current state identifies the preserved later implementation and unmerged review branch; do not restart from older main or replay the archived work queue. Archived plans are historical evidence, not additional approval gates.

Use the single checkout at `Z:\Development\Large Projects\RimWorld Loading Optimizer`. For this campaign, continue from the exact reviewed commit on the preserved `codex/ecosystem-next-plan-review` ancestry; main remains unmerged. Outside an explicitly assigned campaign base, start implementation from current `main` on a `codex/` branch. Do not create another checkout or worktree. One task owns checkout changes, builds, deployment and live launches at a time; read-only reviewers may work alongside it without disruptive scans. Preserve unrelated edits and coordinate ownership before mutation.

Keep project output, decompiled research and raw local evidence under ignored `artifacts/`. The only game fixture is `.rlo-test-instance` in this checkout. Its game, mods, profiles, saves, caches, manifests and results are private ignored data. Never force-add fixture content. Use `git ls-files` or `rg --files` with ignore rules; do not recursively enumerate the repository with PowerShell or use `rg --no-ignore` / `rg -uuu` at its root. Address a specific ignored directory when needed.

Use `scripts/fixture.py` for fixture operations. Never modify normal Steam, Workshop, profile, save or cache state. Live testing must fit the standing GOG permission above or another explicit current grant; a build or metadata exception alone does not grant it. Session-specific overnight workload permission does not establish permanent permission for disruptive medium/full runs. Follow current owner scope and workload limits. Do not use UI automation, interfere with other applications or change security settings.

Functional checks (startup, correctness, activation and similar checks without a performance-benefit claim) may run alongside a freshly verified normal RimWorld process outside the fixture. Only an existing fixture process or uncertain executable identity blocks these operations. Offline work, deployment and preparation use the same fixture-specific check. Performance-measurement launches still require all RimWorld instances stopped. Prepare functional runs with `--purpose functional`; the saved run purpose controls launch admission, and legacy runs without a purpose remain performance runs. Functional-run timings are diagnostic only, not evidence of a speedup. Live launches must fit the standing scope above or another explicit current grant; this never permits closing or modifying the normal game.

Keep the independent menu observer and normal automatic exit for routine comparisons. Use `menuReadyObservation.elapsedSeconds`, wait for actual process exit and capture, and check `automaticTestPassed`. Never start a second test while the first process remains alive. Necessary forced closure is limited to a verified fixture process after preserving failure evidence; it is not a successful test. Manual inspection requires explicit selection and a normal-exit report.

For DLL inspection prefer the existing read-only ILSpy CLI under `artifacts/`. Do not run inline PowerShell assembly-loading or reflective inspection commands, retry permission-blocked CPU profiling, or change security settings to enable them. Use the existing build tool for its approved package identity checks.

Use `git -c core.hooksPath=.githooks commit ...` for every commit. The tracked guard rejects fixture content. Do not push, publish or launch beyond the owner's authorization.

# Product scope and communication

Follow the [development policy](docs/development.md#development-policy). Prioritize shared RimWorld loading work across modlists. The owner also permits targeted improvements for individual mods when they remain soft dependencies: absent, changed or unsupported suppliers must preserve ordinary behavior, with exact identity checks and focused compatibility evidence. The fixture remains a representative workload. Preserve optional dependencies and ordinary fallback behavior.

Check recorded results before pursuing a related experiment. Do not repeat confirmed failures under the same relevant conditions. Reopening a route requires a materially different, evidence-backed premise that addresses its recorded failure; state what changed. Inconclusive tests and untested conditions are not confirmed failures of an entire technique. Outside these restrictions and the operational boundaries above, methods and architecture remain open-ended; current code and suggested next steps are not a fixed roadmap. Make routine decisions within existing task authorization without repeatedly asking permission.

Start explanations with what changed and why it matters. Explain unfamiliar technical terms when needed. Separate source, built package, deployed fixture and captured run facts; menu startup, semantic correctness, gameplay compatibility and speed are different claims. Do not apply the historical combined percentage to the reduced runtime, add overlapping timings or turn a few runs into a universal average.

Prefer the smallest reliable evidence for a product decision. Trust adequate existing validation. Do not expand harnesses, profiling or process documents without a concrete risk or improvement question. Record negative results and stop routes that fail to produce repeatable useful loading gains.

Preserve the separate private/public source histories through the [release workflow](docs/release.md#source-history-and-publication). Do not replace main or merge unrelated histories as an incidental cleanup. Future Workshop content updates must preserve the existing description and unrelated listing fields unless the owner explicitly requests changes.

## Current overnight measurement window — 11 September 2026

Owner grant relayed by the steward at **2026-09-11 05:57:38 UTC** permits isolated
GOG performance testing after functional correctness, with conservative cutoff
**2026-09-11 12:00:00 UTC (08:00 America/Toronto)**. It ends earlier if the owner
returns to PC use or revokes permission. Check time between runs; do not start a
measurement that cannot reasonably finish by cutoff. Let an active verified
fixture process exit/capture normally and exclude contested samples. This grant
is for this window only and supersedes earlier no-window statements only here.

All RimWorld instances must already be stopped for measurements; never close or
modify a normal game or other application to obtain that condition. Pause child
builds/tests/source scans and substantial agent work during measured runs. Use
`fixture.py`, performance purpose, independent observer and normal automatic exit.
Use matched forward/reverse controls and preserve exact identities. C05 first
finishes packaged PSD and native texture correctness, then investigates useful
C05 comparisons; no successor is dispatched by C05. Earlier C01–C04 measurement
obligations remain separate. No Steam, Deck/Linux, UI automation, normal-data
changes, publication, push or merge is authorized.

## Owner returned to PC — 11 September 2026

The owner has returned to PC use and explicitly stopped performance testing.
The overnight window above has expired. No performance runs, microbenchmarks or
performance experiments until a new explicit unattended grant. Standing isolated
GOG functionality/correctness tests and necessary modlist variants remain allowed.

## C10 experiment and further GOG testing approved — 11 September 2026

**Owner approval, 11 September 2026:** the owner approved the bounded C10 native-plugin experiment and explicitly authorized all further testing inside the isolated GOG fixture without repeat testing approval. This includes necessary prototype builds, preparation, deployment and modlist variants. Performance testing remains prohibited during active PC use and requires a current explicit unattended window. This testing permission does not itself approve reduced feature coverage/compatibility, production adoption of an unreviewed backend, Steam promotion, normal-game changes, desktop automation, Deck access or publication. Preserve the existing single-fixture and non-disruptive operating rules.
