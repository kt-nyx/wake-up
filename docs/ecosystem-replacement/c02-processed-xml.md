# C02: processed XML, inheritance and live queries

Wake-Up now reuses completed patch and inherited XML results on restart while retaining native definition construction and custom callbacks. The listed GOG functional checks passed. **Independent steward functional review passed after correction; performance remains pending, so full acceptance is not claimed.** No performance testing was authorized or performed.

Dispatch `wake-up-next-c02-4730cf1-20260910` began at clean `4730cf18542ba984ab63b716f6f3d6d17a66630a` on `codex/ecosystem-next-plan-review`. Main remains `5299ff4974367e5492181f2f72ccd20f91e51629`; prior ancestry is preserved. The C02 orchestrator owns all edits/builds/fixture work. Raw evidence is under `artifacts/ecosystem-next-20260910/c02/`.

## Implemented boundary under qualification

Processed reuse captures a complete private combined-tree generation before an unknown operation receives it. The changed premise from R1 is a substantial mixed stage with exact native and CE/VEF adapters, not restoring every source document to bypass a cheap official prefix. R4b's retained-node mutation journal is not used. Existing source assets remain intact. Completed combined roots, detached mapped originals, source bindings, interned names and exact operation state are reconstructed before publishing document/map references.

Known settings/mod-name wrappers execute once in native order. A recorded call tree preserves the originally selected native conditional branch and sequence short circuit. Reviewed pure XML workers are skipped. Unknown operations end admission and execute normally; interception ends before the unknown suffix. Native ConfigErrors, TKey lifecycle, definition conversion and Complete remain. Cold diagnostic output refuses generation publication rather than replaying logs.

Setup occurs inside an explicitly ordered native `ApplyPatches` prefix, after the reviewed Mod Settings Framework loading callback and before query scope creation. The outer loader bridge returns the resulting document/map references. Exact Prepatcher conversion callbacks still bracket native definition conversion. Their original method bodies and foreign hooks are checked individually. Empty expansion definitions at startup are bound without running metadata getters early. Snapshot schema 3 records actual CE gun `HasActiveModWithName("RunAndGun")` calls; warm traversal repeats those native lookups, preserving lazy cache effects without repeating completed XML edits.

Inheritance remains separately selectable. Native registration, dependency filtering, parent selection, traversal, duplicate/cycle diagnostics and downstream consumers run. The cache substitutes only complete detached child clone/merge results. No-parent roots retain native identity. Actual patched inputs, registered objects/source ownership, mod order, duplicate-field policy and effective method contracts bind each generation.

The live query extension caches completed `SelectSingleNode(string)` results within the active patch document, including connected custom-call contexts. Every XML event invalidates results and misses. It also reuses compiled XPath syntax instructions while retaining the native evaluator, fresh iterator and lazy node-list construction. Syntax instructions contain no selected nodes and remain usable after XML changes. The old OR planner, eager suffix replacement and CE-specific hook were not restored.

All three ordinary selections (`processedXml`, `resolvedInheritance`, `xmlQueryExtensions`) default off. Stores participate in the shared configurable 1 GiB initial total, with existing owned-store maintenance, replacement accounting and disabled clear. Processed/inheritance selections are independent of parsed reuse. Hot reload retains the native path.

## Evidence and remaining work

Initial metadata audit matched GOG generation `gog-rev573-20260910-194017-cf1a1eb0`, manifest `b044a0670562480e281cc95a542f664bec66b7e5703545cbd528f334203bd2ef`, game DLL `4a170804fbfefabdb620d8914e584e58f822a58c6e304dcb76a67003588dab28`, original three exclusions and the C01 deployed endpoint. No transition was needed. Initial identities are saved separately from later packages/captures.

The first integrated suite passed 148 managed checks. The final focused implementation check (`thirteenth-tests.log`) passed 29 managed and all 61 Python fixture tests. Earlier invocations encountered intermittent access-denied errors in pre-existing temporary Steam-transition test-directory renames; no fixture transition was attempted. The ordering, native lazy-list behavior, late-hook fallback, mutable patch state, source/name-table preservation and bounded effect-count formats have focused coverage.

The original seven-run product source/package is `9e03e0d976cafe3e29528147cdffb49b3a82245a`, core DLL SHA-256 `a8f9bebaaa8bce529580f2df0e11a67be5fd51fb31ee8cc7cee2ba8db030b95e`. The independent consumer observer is built from `3d86b1636976678407583e9ba5b7e55c9ddd0f07`, DLL `b1fa3c6541529ef92763af2599c59defbf8ffeb484b267d9c0c6c273b3ed0165`. Those seven captures describe that package; the subsequent review correction and its separate runtime evidence are recorded below.

| Functional evidence | Result |
|---|---|
| `c02-native-07`, `c02-cold-07`, `c02-warm-07` | All normal exits, `automaticTestPassed=true`; same package, observer and mixed official/VEF/animals/plants/CE selection. XML-cache suppliers are absent. |
| Completed patch reuse | Entire stage admitted: 3,011 top-level / 3,699 total described operations, including 68 custom operations. Warm skips 2,998 intercepted patch calls, executes 39 required wrappers and repeats all 43 recorded CE mod lookups. |
| Inheritance | Cold stores 8,205 children; warm restores all 8,205 with zero native child merges. |
| Downstream equality | Native/cold/warm hashes and ordered counts match for 18,618 definition consumers, 114,471 resolved-node reads, 3,011 operation-state rows, 2,021 original-source rows and 5,620 sampled ThingDefs. No observer error or source-change flag. |
| Cold live queries | 2,989 syntax plans compiled once, 1,054 repeated compilations avoided, zero plan fallbacks. The separate single-node memo had only one hit; it is not the basis of the useful cold-coverage claim. |
| Unrelated Alpha Animals list | `c02-alpha-cold-07` / `c02-alpha-warm-07`: full 121-top-level / 569-described-operation stage; 108 intercepted calls skipped, 20 wrappers run. All 5,244 inheritance results reused. Ordered downstream partitions match: 15,162 consumers, 95,096 resolved reads, 121 state rows, 1,890 sources, 4,077 sampled ThingDefs. |
| Foreign inheritance hook | `c02-foreign-observer-07` adds Adaptive Storage. Its `bs.postinheritanceoperation` transpiler is identified; processed and inheritance reuse both retain native processing. All 5,246 child merges run natively; no inheritance generation is published. Normal exit and zero observer errors. |
| Colony/save smoke | `c02-save-smoke-07` reuses the full mixed caches, creates a 75×75 map and three colonists, runs 601 ticks, saves/reloads and passes paused/unpaused native tick checks. `gameplayTestPassed=true`; not a background-hold or long-session compatibility claim. |

The ignored `compare-native-mixed-07.json` records every comparison partition. The observer samples selected ThingDef fields, not every possible gameplay property. Earlier cold captures 01–06 documented conservative refusals and incremental admission; they are not the final functional endpoint. In particular, 31-operation and 286-operation prefixes were insufficient and were extended before handoff.

All seven final-package captures exited normally with `automaticTestPassed=true` and zero observer errors. `final-run-summary.json`, the two comparison files and individual run records preserve settings/content/package distinctions. The private observer reads native downstream consumers so instrumentation does not create a producer-observer exception in the product. Parent/source changes bind new keys, malformed payloads fall back before publication, and the original native diagnostic instructions remain on miss/refusal paths. Arbitrary custom operations and observers remain outside the finite contract and retain ordinary processing; this is not universal mod compatibility.

## Restored handoff

Task `01a08def-0404-7e21-8a9a-a7bc6fdb999c` returns exclusive checkout/build/fixture ownership to steward `01a08d63-a8ef-7c40-8cd9-bbf9f512ce22`. All child agents and operations are stopped. `restore-profile` transaction `20260910-215735-36d5e93f` restored the GOG seed. Original `ModsConfig.xml`, `Prefs.xml`, exclusion metadata, generation pointer and game DLL match the initial hashes. Missile Girl and Loading Progress are restored; the original three exclusions remain. Wake-Up is not enabled, its settings and prepared pointer are absent, and no RimWorld process remains. The qualified GOG core/observer remain deployed. Final audit and stopped-state receipts are under the ignored C02 evidence directory. Normal installations, Deck, security settings, parent coordination state and timers were untouched. No merge, push or publication.

Independent steward coverage/correctness/compatibility review is still required. The same C02 task remains available for corrections and later measurements. Performance-pending alone does not block the next slice after that functional review and ownership transfer.

Later measurement must compare native, parsed only, processed only, inheritance only, all XML features and query extension on cold/uncached mixed regions, with first-construction and warm generations separated. Complete validation/storage/materialization and native endpoint costs remain included. Query savings overlap patch bypass and must not be added. Individual and combined performance remains pending an explicit unattended window.

## Query-hook review correction — 10 September 2026

Independent review of handoff `29ec886ca7348442d7692a17a63219e71f479295` found that a remembered single-node result could bypass another mod's changes inside XPath evaluation. The steward returned exclusive ownership to the same C02 task under `wake-up-c02-review-query-hooks-20260910`. The original seven-run evidence remains supporting evidence for `9e03e0d`; it is not reattributed to the corrected DLL. No further processed/inheritance blocker was returned.

Product commit `ba17b033479f28fb5902be7ff9e5da8ac4ad1bc8` makes the single-node cache independently check the complete native query contract before installation, reads and captures. Both query optimizations share the description of guarded `Select`, `Compile` and `Evaluate` methods, but retain separate guard instances and installation state. Single-node checks also cover the native string wrapper, document navigator factory and lazy node-list `Item`/`ReadUntil` links that a cache hit bypasses. A foreign hook therefore retains native evaluation even if syntax-plan installation was refused. No owner-name exception was added.

Four actual `XmlNode.SelectSingleNode` callsite regressions attach a foreign evaluator prefix that both counts calls and changes the selected result. They cover the hook before installation, after installation but before population, after a cached capture, and between memo installation and refused plan installation. All four fail against the prior memo implementation and pass with the correction. The final focused check passed **28 managed tests and all 61 Python fixture tests** (`review-query-regression-before.log`, `review-query-tests-final.log`). Existing syntax-plan tests retain useful reuse without a foreign hook.

The corrected GOG core DLL is `e507575091f92f2b0d1882e828943666d3557cc7c8b0fc4487f2068b590b6f68`, deployed by transaction `20260910-221145-1b54573d`; game generation and observer are unchanged. One minimal functional capture, `c02-review-query-01`, enables only `xmlQueryExtensions` while parsed, processed and inherited XML reuse are off. It exited normally with exit code 0, `automaticTestPassed=true` and zero observer errors. Query reuse remains active: 2,989 syntax compilations, 1,054 plan hits, zero plan fallbacks, 480 single-node captures and one single-node hit. Its observed consumers, resolved nodes, operation state, original sources and sampled ThingDefs match `c02-native-07` exactly across all five partitions listed above. This is startup/correctness evidence, not a new save-smoke qualification or performance result. See `review-run-summary.json` and `review-query-comparison.json`.

Restoration transaction `20260910-221535-7c5ccc43` returns the fixture to its pre-correction seed/order and exclusions. Generation pointer, game DLL, observer DLL, `ModsConfig.xml`, `Prefs.xml` and exclusion metadata hash-match `review-before-identities.json`; only the intentionally updated core differs. Missile Girl and Loading Progress are restored, Wake-Up is disabled, settings/prepared pointer are absent, and no RimWorld process remains. `review-final-audit.json`, `review-final-identities.json` and `review-final-stopped-state.json` record the endpoint.

All editing, build and fixture work is stopped. C02 returns exclusive ownership to the steward for independent re-review; the slice remains **review pending, performance pending, not accepted**. Normal installations, other applications, Deck, security settings and parent coordination state/timers were untouched. No performance experiment, merge, push or publication occurred. The earlier individual/combined measurement obligations remain.

## Steward functional review — 10 September 2026

The steward reviewed original handoff `29ec886ca7348442d7692a17a63219e71f479295` and correction handoff `1a4f355b24934ad185322e1fdc4f69eaf13f85d4`. Independent bounded source reviews covered processed patches, inheritance and live queries. One consequential query issue was found: single-node memo hits could skip foreign downstream evaluator hooks. The same orchestrator corrected it in `ba17b033479f28fb5902be7ff9e5da8ac4ad1bc8`. The steward inspected the correction and four real-callsite regression cases, including memo-installed/plan-refused order, and verified the recorded fail-before/pass-after results. No outstanding functional blocker remains within C02's agreed scope.

The steward independently checked all seven original capture receipts and observer hashes, exact mixed native/cold/warm and unrelated Alpha cold/warm partitions, actual skipped patch/inheritance work, preserved wrappers/lookups, foreign inheritance-hook fallback, and the colony save/reload result. Original source rows were compared by native file identity; other partitions retained observed order. The corrected query-only capture also matches the independently reviewed native partitions and exercises the new package with persistent XML caches off. Final core/observer/game/profile/order/exclusion identities and stopped ownership were checked. Adequate focused tests were trusted; no build, game launch or repeated test suite was performed by the steward.

**Functional coverage, correctness and compatibility review passed; performance pending.** The finite supported native/custom contracts and sampled gameplay evidence do not establish universal mod compatibility. Original seven-run evidence belongs to `9e03e0d`; the correction and query-only capture belong to `ba17b03`. C03 may proceed under the owner's sequencing adjustment. The same C02 orchestrator remains responsible for later measurements/corrections after exclusive ownership transfer. No supplier-suite removal, full acceptance, merge, publication or Steam promotion is authorized by this review. Ignored independent review records are `steward-evidence-review.json` and `steward-correction-review.json` in the C02 artifact directory.

## Overnight qualification — 11 September 2026

Processed-cache restoration had a substantial regression, now corrected. The
final matched set shows a small processed-only warm benefit. Inheritance,
live-query-only and combined XML loading still fail to establish a useful
whole-startup benefit. All capabilities remain implemented and default off;
**C02 is returned for independent review, not fully accepted**. A faster processed
component does not accept a separately regressing inheritance component.

Original C02 owner `01a08def-0404-7e21-8a9a-a7bc6fdb999c` resumed exclusively
under `wake-up-c02-overnight-qualification-20260911` from clean
`6d4fa77896c8d2d38fae56f7775211dc8e508967`. Main, approved `f70aa08`, preserved
`fe687d4` and subsequent C01–C05 ancestry remain intact. No historical checkout
was restored. The existing owner grant began at 05:57:38 UTC and ends at 12:00 UTC
or earlier owner return/revocation. Actual measurements ran from **10:01:55 to
10:52:23 UTC**. All game instances were stopped before each measurement; child,
build, test and source-inspection work paused during measured launches. No system
file-cache flushing or artificial pressure was used.

### Corrections and their evidence

- `8d69f2e81764a2a28bae2fa77f53a5e29b617374` indexes actual source assets, root
  positions and operation entries once. Previously, decoding every source binding
  walked a linked XML child list again, and replay repeatedly searched the full
  operation list. Snapshot bytes, source identities, state and callback ordering
  remain the same. Initial processed warm patch boundaries cost **6,566.6–6,579.9
  ms**; final boundaries cost **898.5–915.1 ms**. Initial warm menu results were
  21.499/22.326 seconds. The intermediate indexed comparison at `8d69f2e` was
  native 16.351/16.681, construction 17.327/17.971, warm 16.022/16.298 seconds.
- `ec0c27e6e4401e512008f1248fc4d49d18477e47` keeps an already durable inheritance
  generation when every reused result came from it. Any newly computed native
  result still permits normal encoding and atomic replacement. Reads still mark
  the store used for maintenance. Before correction, diagnostic performance run
  `c02q-d-inheritance-warm-33` spent 309.9 ms encoding/publishing the restored
  generation again. Corrected warm runs restore all 8,205 children and publish
  **zero** new generations. Full input verification, registration, mutable native
  restoration, callback guards and fallback remain. Their remaining cost still
  exceeds the native inheritance work on this workload.
- `abcf78874591f1ca722efa50bbf75922fec21756` and
  `57c40492c92b41139514b161a8912efec8516dc7` remove child enumerator/work-stack
  allocation from query-tree admission and avoid repeated reflection for common
  exact native node types. Every descendant and attribute is still checked under
  the same node limit; custom nodes and foreign downstream hooks retain native
  queries. Actual memo/plan activity is unchanged. **No repeatable query speedup
  is established.** The direct old/new/new/old performance probe (64–67) recorded
  old menus 16.652/16.371 seconds and new menus 16.505/16.526. Setup counters also
  varied: old 91.818/78.413 ms, new 29.777/182.967 ms. Functional setup observations
  are diagnostic only and must not be substituted for this inconclusive result.

The existing private observer gained only the two outer C02 wrappers at
`b9723180e861f1a8b2408bf8f3cee2bcdcc29bad`: complete patch application and complete
inheritance resolution. Existing opt-in receipts gained bounded phase counters
at `64b4cf7` and `abcf788`. The observer changes no producer admission rule or native
callback. Outer timings include verification/restoration/publication; private
phase counters are parts of those boundaries, not additional time to add.

### Exact final package and functional scope

| Layer | Identity |
|---|---|
| Final product source / built package | `57c40492c92b41139514b161a8912efec8516dc7` |
| Final core DLL SHA-256 | `7e39676569f3525cad1e046544049d7be7ba1cb14375dc6925b1f5571cbcd369` |
| Timing/consumer observer source | `b9723180e861f1a8b2408bf8f3cee2bcdcc29bad` |
| Observer DLL SHA-256 | `95be46a47780c61d062126948012bb71a1e72794ab19eb99711f04caf2dd50d6` |
| Runtime | Unchanged GOG generation, manifest and game DLL listed above |

The initial current-source package was `90f0983`, whose product code matched the
assigned base. Intermediate package identities remain in individual captures;
they are not attributed to the final DLL. Final-package functional query run
`c02q-f-query-cursor-63` and parsed/processed/inheritance cold/warm runs
`c02q-f-xml-cold-37` / `c02q-f-xml-warm-38` match the current native control
`c02q-f-native-01` across all five downstream partitions: 18,618 consumers,
114,471 resolved reads, 3,011 operation-state rows, 2,021 sources and 5,620 sampled
ThingDefs. Earlier indexed and inheritance-write corrections have separate
cold/warm equality checks. Warm reuse still skips 2,998 patch calls, runs 39 native
wrappers, repeats 43 required mod lookups and restores 8,205 inherited children.
Query-only still compiles 2,989 plans, records 1,054 plan hits with no fallback,
and captures 480 single-node results with one hit.

Focused implementation checks passed 47 managed and 63 fixture tests before the
last query-walk change; its final focused checks passed 28 managed and all 63
fixture tests. These include the four downstream-hook regressions. Earlier fixture
test invocations encountered the already recorded temporary-directory rename
`WinError 5`; stopped retries passed without harness/security changes. An early
prepare and a test retry were refused while the prior fixture operation held its
lock; neither started a second game. Their failed command logs remain preserved.

There are **66 normal automatic successes: 11 functional and 55 performance**,
including diagnostic comparisons. Of these, 29 use the final core (three detailed
functional, 24 final-matrix timing and two direct query diagnostic captures).
Every timing report completes without installation failure, unfinished scopes or
overlap. Timing-only runs omit the detailed semantic observer. This is current
startup/selected-definition evidence; the original unrelated-list, foreign-hook
and colony/save observations remain bound to their historical packages. It is
not a new universal compatibility or ordinary gameplay qualification.

### Final matched performance set

All rows below use final core `57c4049`, observer `b972318`, the same real mixed
official/VEF/animals/plants/CE selection, language, quality and ordinary retained
features. Gagarin, FastLoader and DefLoadCache are absent. `orderedInput`, language,
texture and other newer optional features are off. Native means these XML options
are off while the other retained Wake-Up behavior stays identical. Every launch
uses performance purpose, minimized nonactivation, independent menu observation
and normal automatic exit/capture.

The exact order is in `final-series.json`: native, forward feature arms, reversed
feature arms, native. Every warm run independently imports its matching captured
construction seed through `fixture.py`; it does not borrow the preceding live
profile. Seeds are processed 40, inheritance 42, both 44, parsed 47 and combined
49. Full labels use prefix `c02q-p-final-`; run/seed hashes and settings are in
`collected.json`. Empty application stores do not mean a cold Windows file cache.

| Selection | Construction menu seconds | Warm / per-launch menu seconds |
|---|---:|---:|
| Native controls, 39 / 62 | — | **16.133 / 16.135** |
| Processed only, 40 / 41 / 60 / 61 | 17.593 / 17.208 | **15.929 / 15.889** |
| Inheritance only, 42 / 43 / 58 / 59 | 17.671 / 17.266 | 17.403 / 16.849 |
| Both C02 caches, 44 / 45 / 56 / 57 | 18.203 / 18.427 | 16.326 / 16.550 |
| Query extension only, 46 / 55 | No persistent store | 16.378 / 16.353 |
| C01 parsed only, 47 / 48 / 53 / 54 | 17.964 / 18.222 | 17.035 / 17.133 |
| Parsed + processed + inheritance, 49 / 50 / 51 / 52 | 19.428 / 19.681 | 16.946 / 16.793 |

Processed-only saves about **0.20–0.25 seconds in this final set**, after paying
extra construction cost. It is a small workload-specific margin, not a universal
average. Its complete patch boundary improves from native 1,170.0/1,425.9 ms to
898.5/915.1 ms. Inheritance-only still costs 868.5/910.7 ms versus native
217.6/352.6 ms. The individual regression is not accepted through combination.
C01's warm source-plus-combination cost remains higher too: 820.9/1,017.6 ms versus
463.7/473.3 ms natively. These are consecutive source scopes, not the whole XML
lifecycle; menu time includes the rest. Do not add stage differences, nested
counter values or query savings to the menu result.

### Restored endpoint and remaining obligations

`restore-profile` transaction `20260911-065256-9b81ef21` restores the assigned
seed/order/preferences and original exclusions. All seven original hashes match,
including original deployed core `ba17b03` and observer `3d86b16`; these restored
binaries are separate from the retained final tested package. Missile Girl and
Loading Progress are restored. Candidate/observer are disabled, settings/prepared
pointer/helper are absent, and no RimWorld process remains. Proof is in
`restored-identities.json` and `closeout-proof.json` under ignored
`artifacts/ecosystem-next-20260910/c02-qualification/`.

`qualification-proof.json`, `collected.json`, the five functional comparison files,
test/build logs and captured runs retain the complete evidence. All operations
and child work are stopped; exclusive ownership returns to the steward. Normal
game/data, other applications, Deck, security and parent state/timers were untouched.
No successor, merge, push, publication or platform promotion occurred.

Independent review of these corrections and the limited processed benefit is
required. **Inheritance, live-query useful performance, full C02 acceptance and
combined release readiness remain open.** Further inheritance work must address
full input verification/restoration relative to its small native budget; merely
repeating the same format or counting avoided merges will not resolve it.
Broader query benefit and C12 callsite coverage, other workloads, later gameplay
and combined C03–C05 measurements remain separate obligations. No capability,
default or dependency was removed or narrowed to obtain these results. The
remaining window belongs to the steward's next decision and expires at the
original cutoff; this handoff creates no new grant.


## Steward qualification review — 11 September 2026

**Functional re-review passed; full C02 acceptance remains open.** The steward
reviewed clean handoff `3bc8b182aa8c607a9ac1b9733de0cece9cd5458b` against
assignment `6d4fa77896c8d2d38fae56f7775211dc8e508967`. Product, tests, validation
and scripts equal final tested `57c40492c92b41139514b161a8912efec8516dc7`.
Two bounded independent source reviews found no blocker: source binding indices
preserve actual asset identity and order; replay flags use the same descriptor
indices as the trace; newly computed inheritance results still publish; and the
query walk visits descendants and attributes under the original node limit and
foreign-hook guards. No repeat build or test was needed for these reviewed changes.

The steward independently rehashed 30 capture manifests (29 final-core captures
and the native functional control), their observer/runtime evidence, and all seven
current restored endpoint hashes. Three complete final functional observation
files are byte-identical to the native control, covering all five downstream
partitions. Source/package/observer identities, normal automatic exits and menu
values agree with the reported evidence. No RimWorld process remains; both source
reviewers and the orchestrator stopped. Ignored proof is
`artifacts/ecosystem-next-20260910/c02-qualification/steward-qualification-review.json`.

Processed-only shows a small measured workload-specific improvement of about
0.20–0.25 seconds in the final balanced matrix, with slower first construction.
This is evidence of benefit in that selection, not a universal result or complete
C02 acceptance. Inheritance-only and combined reuse regress; query-only and its
extra traversal comparison do not establish useful improvement. These failures
remain assigned to the same owner for later redesign/qualification. Preserve
source verification, independent mutable results and callback effects; a new
approach must address their measured cost without weakening correctness. No
capability is excluded, and the combined total cannot accept a losing component.

Ownership has returned. The next serial qualification will examine C03's actual
cross-mod input work on this current ancestry within the remaining existing
unattended window. C01, C02, C04 and C05 retain separate obligations; pending or
failed performance does not block later functional implementation. Full supplier
replacement, release acceptance, Steam promotion, merge and publication remain open.
