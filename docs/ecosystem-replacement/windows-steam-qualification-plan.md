# Windows Steam qualification — authorized 22 September 2026

Executed result: see the [22 September qualification report](windows-steam-qualification-20260922.md).
The exact target was confirmed, the concrete texture admission refusal was corrected
under the closeout grant, bounded functional/UI/performance work completed and GOG
was restored. The parent independently accepted the bounded Windows result at
`66b23eb2`. The sequence below
records the authorized starting plan, including its original candidate identity.

The owner approved isolated Windows Steam qualification and necessary retained-candidate
closeout while leaving the PC overnight. The latest AGENTS notice supplies the
window and boundaries. Atlas retirement is accepted; this plan now targets its exact
private package. No Linux/Deck, merge/push/tag/publication approval follows.

## Exact target and boundaries

Use the revision-specific private archive in the
[candidate report](c15a-retained-candidate.md), containing tested GOG core
`36d739b1497c576e74e337e224bb1712f2337661` (DLL SHA-256
`6cdf5fe145760de3c7570a3c0ad5ef8f139d57d32e20a97cf6b4d05b26d74b07`) and qualified
quality helper `ca12d392e0a173b1dd7b87d85b95621166f4aca53860550808319b4b0c285c36`.
The prior reviewed Steam target was `1.6.4871 rev590`; verify its actual source
directory, game version and assembly hash at preflight. If it changed, report that
target change before claiming the old contract applies. Do not update Steam or mods
to obtain a target. All normal installations, Workshop files, profiles, saves and
caches remain untouched; the Steam source is read only.

Use only `.rlo-test-instance` in the existing checkout. Preserve its frozen mods,
recorded GOG generation `gog-rev573-20260910-194017-cf1a1eb0`, captures, supplier
repair, profile and rollback journal. Keep one operating owner. Never close a normal
game or control unrelated applications to arrange a test.

## Proposed bounded sequence

1. **Record and stage.** Verify stopped fixture/process identity and the seven GOG
   baseline hashes, eleven absences, supplier repair and metadata audit. Record the
   selected read-only Steam source. Run the existing
   `fixture.py stage-steam --steam-source <verified-path>` transaction; preserve its
   receipt and GOG recovery units. Do not launch any recovery copy or create another
   fixture. Independently check the staged game contract before deployment.
2. **Update admission, then bind exact binaries.** The current
   `bind-comparison-package` guard admits only public 0.2.1 or historical candidate
   `d584eb36`/DLL `59ed42a6`; it cannot admit this `36d739b1` archive. The new owner approval covers a narrow fixture-admission update pinned to the
   independently reviewed new archive/core hashes, preserving all existing identity
   checks. Do not broaden admission to arbitrary binaries. Then use
   `bind-comparison-package --package` for the retained archive and public 0.2.1.
   Binding copies the DLL unchanged and records the Steam execution contract separately
   from GOG build references; it does not establish compatibility. Build/deploy only
   the matching private Steam observer. If binding or runtime guards refuse the
   exact candidate, stop and report the concrete mismatch; routine concrete compatibility corrections may be implemented and tested under the
   new closeout grant, but never bypass a guard without proving the required contract.
3. **Functional qualification first.** Prepare `--purpose functional` runs with the
   independent observer and normal automatic exits. Start with ordinary-default
   menu startup and a focused combined retained-feature run covering native image
   fallback, processed XML, translation and relevant lifecycle/compatibility checks
   already used on GOG. Compare semantic output to matching Steam native controls.
   Exercise the copied colony's ordinary load, background/focus return and the
   three-launch native → prepared → reset quality cycle with actual stores carried
   forward. Use only fixture UI under explicit approval for that inspection window;
   report agent observation separately from tick/image probes. Do not repeat every
   supplier matrix unless the Steam transition exposes a concrete difference.
4. **Performance only if separately permitted.** If approval includes an active
   unattended interval, use matched absent/public-0.2.1/current ordinary-default
   forward/reverse comparisons on the same small English workload, then bounded
   applicable translation/type-group/PNG comparisons needed for Steam claims.
   Preserve exact settings/cache provenance, independent menu timing and normal
   exits; exclude contention. Do not reopen losing atlas routes, optimize residual
   costs or generalize GOG percentages to Steam. Without that interval, stop after
   functionality and leave Steam performance unqualified.
5. **Restore and review.** After capture and actual process exit, roll back every
   test/deployment transaction and the platform transition in reverse order. Verify
   the original GOG identities, eleven absences, supplier repair and metadata audit.
   Return clean stopped ownership with exact Steam source/core/observer/run/package
   identities and bounded pass/fail results for independent parent review.

Use [the existing fixture procedure](../development.md#isolated-steam-version-and-three-arm-comparisons)
for command details and guards. The approval sought is the bounded transactional
Windows Steam functional qualification above, including its fixture UI checks.
Performance requires an explicit unattended grant with its time limit. The latest
closeout grant includes necessary concrete correctness fixes; it does not approve
unrelated runtime/default changes, normal-data changes or publication.

## Remaining release gates

- Atlas retirement is approved; review the new candidate before any future
  Steam plan refresh. No atlas optimization gate remains.
- Owner-approved Windows Steam qualification and independent review of its actual
  results; GOG evidence alone does not set Steam flags true.
- After Windows Steam proof, separate Linux/Steam Deck authorization and evidence.
  The Windows quality helper does not establish Linux helper support.
- Final release-scope acceptance, then separate merge/push/publication approval,
  preserving private/public histories and existing Workshop listing fields.
