# Development and fixture

## Owner release and next-campaign direction — 22 September 2026

The owner explicitly authorized committing, merging and releasing the confirmed
Windows-tested candidate as 0.3.0. This supersedes older no-merge/push/tag/publication
notices for this release only. Publish the unchanged qualified runtime and optional
Windows quality helper with source/notices, preserving separate private/public
histories. Windows Steam/GOG qualification is complete within recorded workloads;
Linux/Deck remains unqualified. This release authorization accepts that disclosed
scope, not a new Linux test claim or permission to access the Deck.

The owner then explicitly required the mod description remain unchanged. Do not
change release/About.xml description or the Workshop description, preview or
unrelated listing fields; put changes and qualification limits in release notes.
The next steward is to pursue early whole-stage XML reuse, direct single-pass image
decoding and overlapping independent loading work. On-demand loading is explicitly
excluded. Prepare its handoff; do not begin that campaign during publication.
Performance windows are closed; no new live runs are needed for unchanged binaries.
The old campaign timer stays paused. Future publication needs a fresh authorization.


## Owner Windows Steam overnight qualification — 22 September 2026

At approximately 2026-09-22 06:33 UTC the owner left the PC overnight and authorized
Windows Steam qualification plus necessary retained-candidate work toward release
readiness. This supersedes the prior stop-before-Steam and closed-window notices.
The approval includes the prepared single-fixture qualification plan, its narrow
hash-pinned admission update, necessary correctness fixes/builds, fixture UI checks
using the Windows control skill, and bounded performance comparisons after correctness.
No new optimization pilot, retired path or deferred C10/C11/companion is restored.

As a conservative operating limit, finish measurements by 2026-09-22 14:00 UTC
(10 AM Toronto), earlier on owner return or revocation. The owner supplied no exact
cutoff; this is a steward limit. Do not start a sample unlikely to finish by then.
Never close normal games; all must already be stopped for performance. Preserve
other applications and exclude contested samples. Functional runs use existing
fixture-specific admission. Keep one operating owner and the single transactional
fixture, normal installations/data read-only, and restore the GOG baseline afterward.

Windows Steam approval is not publication approval. Linux/Deck remains its separate
later platform decision; do all useful authorized Windows work before raising it.
Do not merge, push, tag or publish. Prepare a brief plain-language comparison against
the last actual version-tagged release, verified locally, when qualification closes.
Resume the steward timer, review actual changes/evidence, and stop only on completed
authorized readiness or a concrete human-only blocker, not merely a subtask handoff.

## Owner atlas retirement and return to PC — 21 September 2026

The owner approved retiring static atlas caching and atlas batching, and requested
cleanup before stopping ahead of Windows Steam qualification. Remove their release
activation and optimization requirements; use native atlas behavior. Preserve shared
foundations needed by the retained loading display and keep research/history. This
is not approval to remove the display or other retained features.

The owner is using the PC again: the overnight performance and fixture desktop-control
window has ended. No performance measurements, microbenchmarks or desktop automation.
Standing isolated GOG correctness permission remains, with the established fixture-
specific process guard and incidental-focus allowance; use only focused checks needed
for this cleanup. No Steam qualification or admission update, Deck, normal-data edits,
merge, push or publication. Refresh the private candidate, independently review it,
then stop and pause the steward timer before Steam. No new optimization pilot.

## Owner overnight closeout authorization — 21 September 2026

At 2026-09-21 07:06 UTC (03:06 Toronto), the owner left the PC overnight and
asked for autonomous testing, fixes and iteration toward a release-ready retained
candidate by roughly 10–11 AM. Performance testing and Windows control skill UI
interaction are explicitly authorized for this window. The proposed new PNG
optimization is explicitly excluded; C10/C11/companion and the six retired paths
remain deferred/retired. This notice supersedes older no-performance, no-desktop-
automation and owner-only visual-inspection limits for the authorized fixture work.
Record agent-observed UI evidence honestly, not as owner inspection.

Use 2026-09-21 14:00 UTC (10 AM Toronto) as a conservative measurement cutoff,
ending earlier on owner return/revocation. Do not start a sample that cannot finish
before cutoff. The unrelated project may continue; do not control its applications.
Exclude resource-contested measurements, pause our competing builds/scans during
measurement, and preserve all game/process guards. Never close a normal game to
obtain idle measurements. Use the single fixture and serial ownership; keep normal
data untouched. Separate platform-promotion and merge/push/publication approvals
remain unchanged; complete all useful currently authorized GOG work before surfacing
any remaining platform decision. Routine fixture UI checks no longer need owner
participation. Resume the five-minute steward timer and avoid needless process work.

**Owner-approved foreground exception, 19 September 2026:** proceed with isolated
GOG fixture correctness tests while the owner uses the PC. The owner explicitly
accepts incidental fixture focus and will put it back in the background. This
resolves the minimized-launch coordination block for the functional batch. Do not
manipulate desktop focus or other applications. No performance measurements,
owner-assisted visual acceptance, Steam/Deck promotion or publication follows.

**Current boundary, 19 September 2026:** all unattended windows are closed. No
performance runs or microbenchmarks. GOG correctness remains authorized; the owner
accepted incidental fixture foreground for the current functional batch above.
C15A retires the six approved paths and C10/C11 activation; historical flags do not
reactivate those paths in this candidate. See the latest execution ledger.

**Historical unattended window — 13 September 2026 04:49:27 UTC:** the owner
authorized needed performance tests while the PC is unattended. No fixed cutoff
was provided; stop starting measurements on owner return or revocation. This
supersedes older window/return notices only while active. Necessary GOG correctness
comes first for the affected behavior. All game instances must already be stopped
for measurement, with serial ownership and no competing builds or substantial local
work. Incidental fixture foreground no longer interrupts the absent owner; desktop
automation and claims of owner-assisted inspection remain excluded. Scope deferrals
and separate Steam/Deck/merge/publication gates remain unchanged.

**Historical owner unattended window — 12 September 2026 17:35:27 UTC:** outstanding
performance tests are now permitted until owner return or revocation; no fixed
cutoff was specified. This supersedes earlier no-window statements while active.
The C10 owner first finishes public-patch correctness, then measures bounded
natural audio cold/warm behavior. Existing process isolation, matched forward/
reverse controls, serial ownership and platform limits remain mandatory.

**Owner-approved sequencing update, 10 September 2026:** continue through slices after independent functional coverage, correctness, compatibility and expected-behavior review. Performance testing is deferred until an explicit unattended window. Performance pending alone does not block a successor; it still blocks full acceptance. Retain exact feature/source/package identities for later individual and combined measurements, and transfer the single checkout's ownership before returning corrections to an earlier orchestrator. All operating and platform-promotion limits below remain in force.

This is the reusable development and isolated-test procedure. The combined
source now has bounded [F1 fixture evidence](archive/ecosystem-replacement-20260910/rc2-functional-qualification.md)
after a live integration fix. Public 0.2.1, the older local candidate and the
original unfixed rc.2 ZIP retain their separate identities. Read
[current state](current-state.md), the [proposed plan](ecosystem-replacement/next-campaign-proposal.md)
and [architecture](architecture.md) for current facts and contracts.

## Explicit ASTER coexistence for functional work

When the owner confirms a normal game is on another ASTER workstation, add
`--allow-other-session-game` to the authorized functional fixture operation.
This opt-in tolerates an unavailable executable path only when a fresh Windows
query identifies a different, nonzero session. Identifiable fixture paths and
recorded fixture process IDs still refuse; same-session or unknown-session games
still need executable identification. Performance launches still require every
RimWorld process stopped, regardless of this option. No PID exemption is saved.
The option does not change security, focus or the other game's state.

## Standing campaign testing permission

**Owner approval, 11 September 2026:** the owner approved the bounded C10 native-plugin experiment and explicitly authorized all further testing inside the isolated GOG fixture without repeat testing approval. This includes necessary prototype builds, preparation, deployment and modlist variants. Performance testing remains prohibited during active PC use and requires a current explicit unattended window. This testing permission does not itself approve reduced feature coverage/compatibility, production adoption of an unreviewed backend, Steam promotion, normal-game changes, desktop automation, Deck access or publication. Preserve the existing single-fixture and non-disruptive operating rules.

**Owner instruction, 10 September 2026; effective unless overridden to the steward.** The next implementation plan was approved on 10 September 2026; the workflow may perform necessary functionality/correctness tests using the **GOG game** and different modlist workloads inside the single `.rlo-test-instance`. This includes required isolated builds, deployment and preparation. Orchestrators inherit this permission; do not ask again for covered operations. Record exact game/reference, package, observer, content/order, language and profile identities. Begin with the smallest useful workload; the current CLI lanes and reversible exclusions remain the existing tools, not a restriction to one content family.

**No performance testing unless the owner explicitly states that the PC will be unattended for a currently applicable window.** When the owner is using the PC, or no such statement exists, run only functionality/correctness checks. This prohibition includes CPU/helper/codec microbenchmarks, overhead measurements and speed experiments as well as whole-game comparisons. Functional diagnostic durations may reveal a timeout/hang but cannot qualify gains, regressions or overhead. A functional pass awaiting measurement remains pending, not accepted.

The steward records the owner's unattended message, duration/limits and subsequent overrides and passes them to each orchestrator. Old overnight permission, silence, apparent inactivity or a recurring check does not grant a new window. Stop starting measurements when the window ends or the owner resumes PC use; preserve and exclude a sample affected by resource competition. Let an active fixture process exit/capture normally. Do not stop the normal game or other applications to manufacture an idle host. Performance launches require all normal RimWorld instances already stopped; builds and substantial other local work pause during measurements.

Use `--purpose functional` and minimized nonactivating launches where supported while the owner uses the PC. Preserve focus, normal data and other applications. Visible/audible/focus checks that would interrupt the owner need coordinated owner-assisted inspection; no UI automation. Restore the recorded pre-assignment isolated profile and workload after testing, and retain captures. A verified normal game outside the fixture can coexist with functional checks under the existing guards; uncertain process identity or an existing fixture process blocks another launch.

C01 restored the complete verified GOG runtime in generation gog-rev573-20260910-194017-cf1a1eb0 through transaction 20260910-194136-685716c0, using committed tooling ba04b27. Original GOG and later Steam recoveries remain preserved. Matching GOG core/observer builds are required before live work. This standing GOG permission does not newly authorize Steam tests, normal-game changes, Workshop/Deck access or publication. Specific existing grants apply only to their recorded scope; the completed F1 campaign is not an ongoing queue.

Implementation is now approved. The prior documentation review performed no build or fixture operation. The [execution ledger](ecosystem-replacement/execution-ledger.md) identifies the active slice and the owning orchestrator; the approved [plan](ecosystem-replacement/next-campaign-proposal.md#standing-operational-permission-and-pc-availability) carries the same operating policy into assignments.


**Platform promotion order:** develop and validate all features on GOG, then validate them together as a combined candidate on GOG. Only after that gate may the owner explicitly approve promotion of the release candidate to isolated **Windows Steam** testing. Do not rotate routine slice development between GOG and Steam. The single fixture transitions transactionally and keeps its captures/recovery data; normal installations remain untouched. **Linux/Steam Deck** is a further, separately authorized promotion after Windows Steam behavior is proven; Linux helper work remains deferred from this Windows campaign. Every performance test at every stage still requires a declared unattended window. See [the promotion gates](ecosystem-replacement/next-campaign-proposal.md#platform-promotion-and-later-release-validation).

## Development policy

The objective is faster, reliable RimWorld loading across modlists. Prioritize shared loading work. As of the owner’s 2026-09-07 scope expansion, targeted loading/UI preparation improvements for individual mods are also allowed when implemented as soft dependencies: the mod must remain optional, and changed or unsupported supplier code must fall back safely. The recorded 0.2.1 runtime checks required game/supplier functions per feature instead of rejecting whole game or supplier revisions. Harmony remains pinned. Preserve ordinary fallback; do not restore whole-game admission restrictions. Exact identities still matter for reproducible build references and evidence. The fixture remains a representative real-world workload. A general mechanism may help some workloads more than others; it need not produce the same gain in every modlist.

Read the [proposed roadmap](ecosystem-replacement/next-campaign-proposal.md) for current priorities and the [archived results and decisions](archive/results.md) before pursuing a related idea. Do not repeat an experiment that has already established no useful benefit under the relevant conditions. Reconsider it only when materially new evidence, a different mechanism or a relevant change in conditions addresses the recorded reason it failed; briefly record what changed and why that could alter the outcome. Renaming an old approach or rerunning the same comparison is not a new premise. Keep confirmed negative results separate from invalid tests, inconclusive measurements and untested conditions—for example, the DDS warm-start result does not settle first-read performance.

Outside these restrictions and the operational boundaries below, development is open-ended. No particular algorithm, caching strategy, concurrency model or architecture is prescribed or permanently ruled out as a whole. The current implementation and suggested next steps describe a starting point, not a required roadmap. Choose investigations and implementation methods from evidence about avoidable or overlapping loading work, likely user benefit, effort and compatibility risk.

Use enough verification to answer the actual question, and trust adequate existing evidence. Do not retain complexity merely because it was implemented, or expand testing, profiling and process into separate projects without a concrete need. Within an authorized task, routine design choices, focused checks and revisions do not require repeated permission. Use the standing scope above for launches; these development policies do not grant publication or changes to normal user data.

## Workspace and ownership

Use `Z:\Development\Large Projects\RimWorld Loading Optimizer` as the only checkout. Read [current state](current-state.md) before choosing a runtime base. Preserve the reconciled source and both histories; the existing source-history procedure is in [release](release.md#source-history-and-publication). For this campaign, begin from the exact reviewed documentation commit on the preserved fe687d4 ancestry, then the steward-assigned accepted slice base. Keep main unmerged. Use `codex/` branches and one owner for edits, builds, deployment and launches; do not replace the assigned base with older main. Read-only review may run alongside preparation; pause other substantial local work during measurements. Keep research, build output and raw evidence under ignored `artifacts/`. Use the tracked commit guard:

```powershell
git -c core.hooksPath=.githooks commit -m "Describe the completed change"
```

The only fixture is `.rlo-test-instance` at the checkout root. It contains private copies of the game, frozen mods, an isolated profile and captured results. Do not create another fixture or write normal Steam, Workshop, profile, save or cache state. Do not refresh the frozen collection merely to prepare another run. Routine preparation checks existing metadata; full content audits are a separate operation for a specific drift question.

## Naming and retained historical identities

The formal title is **Wake-Up: Loading Optimizations**; ordinary prose and the
settings menu use **Wake-Up**. The repository and release folder are `wake-up`.
C# projects, namespaces and the runtime DLL use `WakeUp`; build environment
variables start with `WAKE_UP_` and development arguments with `--wake-up-`.
Diagnostics and processed textures live under the game's `WakeUp` data folder.

The existing checkout path and `.rlo-test-instance` location remain fixed.
Private fixture package IDs, directory names and v1 record schemas retain their
original spelling so frozen inventories, rollback transactions and captures
remain valid. Historical reports retain recorded commands and artifact paths;
they are not current invocation examples. Public packaging also names the two
old package IDs solely to warn against installing duplicate copies.

Source, built core, deployed core and captured run are separate identities. Old
captures remain evidence only of their recorded builds.

## Build and offline checks

Use the SDK pinned in [global.json](../global.json) and exact references selected
by the existing build tooling. Do not substitute a convenient installed game DLL.
The current isolated GOG fixture and its reference identity are recorded in
[current state](current-state.md); historical GOG and Linux build contracts remain
separate. From the single checkout:

```powershell
python scripts/fixture.py test
python scripts/fixture.py build
```

`test` restores locked dependencies, builds, runs managed tests and Python tooling
checks without launching the game. `--test-filter` narrows managed checks while
retaining tooling checks. `build` requires committed inputs and creates a
revision-labelled two-file core package and adjacent receipt under
`artifacts/fixture-package/`. Reuse an existing package for that revision rather
than overwriting evidence. Source-inclusive release packaging is a separate
[release procedure](release.md).

The core package contains only `About/About.xml` and `Assemblies/WakeUp.dll`.
Prepatcher is required; Harmony, Gagarin, observer and test DLLs are not bundled.
Build-only Krafs.Publicizer exposes embedded Cecil types in a reference copy;
original dependencies are unchanged. Optional Windows DDS exports use the pinned
helper built by `scripts/build_texture_helper.py`, with one compiler worker.
For this private fixture, invoke helper build/check work inside the existing
`fixture.fixture_lock` and `fixture.guard_operation_processes` protection;
the standalone helper script does not acquire that protection itself.
Existing helper checks can use
`WAKE_UP_TEXTURE_HELPER_TEST_PATH` and `WAKE_UP_TEXTURE_HELPER_TEST_IMAGE`; absent
inputs are a skipped integration check, not a helper pass. Linux helper work is
owner-deferred. See [R2 source/protocol/bounds](archive/ecosystem-replacement-20260910/r2-texture-preparation.md)
and [source-build instructions](source-build.md).

Prefer one useful combined regression selection or the bounded full suite. Focus
additional checks on changed behavior and actual defects, and trust accepted
slice evidence. Translation and full XML-constructor tests use separate modern
CPU hosts because the ordinary test host cannot execute all native engine
methods; see [S03 host limits](archive/ecosystem-replacement-20260910/s03-translations.md#offline-host-limitation)
and [R4a checks](archive/ecosystem-replacement-20260910/r4a-ordered-reads-types.md). CPU results do
not establish actual Prepatcher/Mono execution, Unity behavior or startup benefit.

Shared integration changes must preserve the exact coordinated prepatch/Harmony
hooks, inactive/refused paths, main-thread texture/audio ownership, and native
exceptions and publication order. See the [R3 consumer boundary](archive/ecosystem-replacement-20260910/r3-deferred-assets.md)
and [R4c loading contracts](archive/ecosystem-replacement-20260910/r4c-remaining-coverage.md).
`deferredAudio` is retired and normalized off; `streamingXml` remains experimental and default off.
`hideLoadingSummary` and `loadingTimings` are also off by default. All four are
covered by the ordinary-settings fixture allowlist, without preparing a profile.
The withdrawn `independentXmlReuse` setting is rejected. R1 `XmlPrefix` and R4b
`XmlRegion` sources compile only into tests; neither registers a runtime prepatch
or creates a product storage category. Their losing experiments must not be
repeated under the same premise.

Offline work uses the fixture-specific process guard. Functional launches may
coexist with a freshly verified normal RimWorld process within the standing
GOG scope or another explicit current grant; performance launches require all RimWorld instances stopped. Never
close or modify the normal game. The operation lock, frozen-reference checks,
prepared identities and normal-exit requirements remain in force.

## Explicit deployment and preparation

**Within an approved implementation assignment and the standing GOG scope (or another explicit current grant).** Building does not deploy. Use the exact path returned by a successful build, then check that deployment itself exits successfully:

```powershell
python scripts/fixture.py deploy --package <package-path-returned-by-build>
```

Before preparing a series, read `.rlo-test-instance/candidate.json`: `sourceRevision` must equal the intended package revision. After preparation, require the same revision in `.rlo-test-instance/results/<label>/run.json` under `candidate.sourceRevision`. A receipt or log file existing is not proof that a deployment command succeeded. The tool refuses old packages containing the retired Core DLL.

Build and deploy the separate menu observer after its source changes, or if the fixture does not yet have its matching package:

```powershell
python scripts/fixture.py build-menu-observer
python scripts/fixture.py deploy-menu-observer --package <observer-package-path-returned-by-build>
```

Prepare a unique label. This example enables the two searches in the small activation lane:

```powershell
python scripts/fixture.py prepare --mode candidate --strategy startup-searches --lane activation --label next-candidate --purpose functional --menu-observer --exit-after-menu-ready
python scripts/fixture.py verify-prepared --label next-candidate
```

The supported lanes are `activation` (small official content with the required bootstrap environment), `xml-expanded` (the documented frozen XML-heavy selection encoded by the tool) and `representative` (the frozen enabled order, subject to recorded exclusions). There is no current `presets`, medium or full CLI lane: older records used earlier selectors. Representative is the large comparison route; its functional use and other necessary modlist selections are covered by the standing GOG scope; performance additionally requires an unattended window.

`--mode absent` physically parks Wake-Up outside game discovery and preserves the comparison content order. `--mode baseline` keeps the selected original-path timers. `--mode candidate` enables the selected supported optimization. The strategy defaults to `startup-searches`; `def-lookup` and `type-lookup` permit isolated comparisons. Preparation defaults to timing observation, the independent menu observer and normal automatic exit. It also records the fixture-only background-loading preference.

Explicit fixture controls default Gagarin off; ordinary user settings default it on. `--gagarin-cache timing`, `on` or `verify` requires candidate startup searches. Preparation resets the application profile from its frozen seed. For a matching Gagarin cache-hit comparison, `--foreign-cache-from <successful-captured-label>` restores only the captured MissileGirl cache after checking content, package and observer compatibility. Inspect `foreignCacheFrom` and `restoredCacheKinds` in the run record; do not infer a cache hit from preparation alone. Actual supplier and optimization receipts must confirm it. Do not mix verification mode into only one timed arm.

`exclude-mod --package-id <exact-id>` and `restore-mod --package-id <exact-id>` maintain a reversible fixture selection without editing frozen files. An exclusion changes the workload and must match between comparison arms; it is not an optimizer gain. Follow current task scope rather than excluding mods opportunistically.

If ordinary startup rewrites an inventoried mod file, `restore-runtime-file --package-id <exact-id> --relative-path <inventoried-path>` can restore its frozen content and timestamp after the game exits. The action first verifies that the original source still matches the frozen length and complete SHA-256, preserves the overwritten fixture file in a recovery transaction, and refuses unlisted paths or newer source bytes. It does not refresh the manifest or write the original source. Keep the captured run and restoration receipt together when this maintenance is needed between comparisons.

## Authorized launch and capture

Live testing must fit the standing GOG scope or another explicit current grant. For a startup/correctness check, include `--purpose functional` when preparing its fresh label. Use `--purpose performance` explicitly only within a declared unattended window; omission is treated as performance by existing tooling, not permission to benchmark. Launch uses the purpose saved in `run.json`, not a new command-line override. Within that scope, the prepared label is launched with:

```powershell
python scripts/fixture.py launch --label next-candidate --authorize-live-launch
```

For a non-disruptive functional launch, add `--window-style minimized-noactivate`.
It requests Windows `SW_SHOWMINNOACTIVE` at process creation and never restores
or foregrounds a window. F1 verified a minimized, nonforeground fixture window
and successful native menu observation. This request is functional-only; it is
not a universal guarantee for changed game/window implementations.

`deploy-helper --package <reviewed-rc2-ZIP>` is a narrow fixture-only overlay.
It accepts only the pinned reviewed ZIP/helper hashes, copies only the Windows
helper into the existing private mod, preserves the current core, and records
independent origins. It does not install the public mod or make the old release
ZIP match a repaired DLL. The ordinary operation lock/process guard and
transactional recovery still apply.

The launcher rechecks the prepared identity, profile, package and order. It waits for actual process exit and captures evidence before returning. Do not build, deploy, switch branches or start another run during a measurement. Do not use UI automation or interfere with another application. A label in an old document is historical; the authoritative selection is the current `prepared.json` and its matching run record.

The observer marks menu readiness after the first completed main-menu repaint. Both the launcher and observer use the Windows high-resolution counter, so polling delay does not inflate the reported elapsed time. The observer writes `profile/FixtureMenuObserver/menu-ready.json` and logs its event. One second later it requests the game's normal shutdown from an update callback, only while still at the startup menu with no game loaded. The one-second delay and shutdown time are excluded from loading time.

Inspect the captured `results/<label>/run.json`, profile log and relevant Wake-Up receipts. A routine automatic success requires a valid `menuReadyObservation`, actual normal exit code zero and `automaticTestPassed=true`. Use `menuReadyObservation.elapsedSeconds`, not process lifetime, for complete startup. Installation or activation refusal is not a candidate result.

The existing bounded failure handling stops a run sequence when there is no menu signal after 30 minutes or no process exit within 60 seconds after the menu event. It records failure and leaves the process for investigation. Do not start another launch while it remains alive. Preserve evidence before any necessary forced closure, verify that the exact process belongs to this fixture, and treat forced exit as failure. Use the tool's `capture` action for a verified exited run if automatic capture did not complete; do not fabricate a success or overwrite an existing capture.

For authorized manual inspection use `--no-exit-after-menu-ready`, then wait for a normal-exit report. The normal automatic mode requires no human closure report. The observer works with Wake-Up absent and must match between ordinary comparison arms. Its historical small-profile qualification is described in the [history index](archive/README.md#observer-history).

## PNG/JPEG qualification and cache reuse

The optional cache covers native PNG/JPEG output and preserves authored DDS
precedence. `jpegCalls` separates JPEG attempts from aggregate cache counters.
Historical rendered-output checks belong to their recorded core; new combined
behavior still needs authorized qualification. Windows Direct3D 11 compute/CPU
and native Linux OpenGL CPU routes remain implemented; Linux compilation is not
current Linux execution evidence.

The following feature switches are explicit fixture controls, not ordinary user-setting defaults. Use `--png cache` with candidate `startup-searches` to enable the retained processed-PNG cache; its default is `off`. `--png control` records original PNG processing and `--png verify-cache` adds sampled rendering comparisons. Normal DDS precedence is preserved. Only an explicit `--png-source original` qualification comparison selects existing PNG siblings instead of their DDS counterparts. Match that source choice between arms and report it as a PNG workload, not a gain for the normal DDS-heavy fixture.

`--png-cache-from <label>` restores only a normally completed capture's processed-PNG entries after checking file identities and matching package, content, lane, selectors, profile overrides and observer. The first cache build and subsequent hits are different performance conditions. Keep all source and payload validation inside startup timing; verification replay is a separate correctness arm. The cache needs no external conversion tool or mod-specific allowlist. Unsupported graphics/settings retain original processing.

## Character Editor preset preparation

`--character-presets on` enables optional startup preset lookup reuse in candidate `startup-searches`; `timing` measures the original preset construction with the same receipts, and default `off` installs nothing. The runtime selector is `--wake-up-character-presets=on`. Character Editor is not a required package or assembly reference. In the 0.2.1 baseline, required function signatures and method bodies plus the relevant patch chain determine admission; version 1.6.3.3 identifies the historical supplier evidence rather than a whole-DLL runtime restriction. Updated or absent suppliers retain ordinary loading. This does not pin a Workshop subscription or change supplier files.

The original preset constructors and public dictionary getter remain intact. Only the first startup object/turret creation scopes may reuse the original lookup dictionary; later calls, changed definition lists, nested/cross-thread calls and foreign patches use fresh original lookups. Reuse ends at the loaded menu. Inspect `character-presets.jsonl` for scope counts, preset-content hashes, actual hits, no exceptions and zero retained entries after completion.

## Independent parsed XML

C01 adds ordinary `parsedXml` selection (default off), shared `sharedCacheMiB`
(initially 1024), and one owned `WakeUp/ParsedXml/v1` generation store. Existing
cache maintenance applies, including clear with selection off. See the
[C01 record](ecosystem-replacement/c01-independent-parsed-xml.md) for production
boundaries, binary representation, fallback and the exact qualified workload.
The fixture supports identity-checked `--parsed-xml-cache-from LABEL` restoration.
The separately opt-in `--xml-source-observer` is a private functional automatic
probe of a foreign source observer; it is never a performance arm.

## Loading display, summary and timing diagnostics

The ordinary loading display, display diagnostics, native-summary hiding and
per-mod XML timings each default off. Explicit selectors are
`--wake-up-loading-display=on`, `--wake-up-loading-display-diagnostics=on`,
`--wake-up-hide-loading-summary=on` and `--wake-up-loading-timings=on`.
Display formatting is limited to four updates per second and native repaints draw
cached content. Loading Progress owns its screen when active; exact drawing
contracts independently govern Wake-Up display and summary suppression.

The selected timing option also writes a separate bounded `WakeUp/early-loading.txt`
report around actual Mod constructors, starting before the first constructor.
It does not move or repeat construction and is not the full profiler.

The XML report bounds 512 records and 32 open scopes. It measures XML input and
patch-object construction after Wake-Up initializes, not all startup work. Nested
or overlapping timings must not be added as wall time. First-launch completion
is observed after the native Entry queue drains. Continue using the independent
menu observer for startup comparisons; report and display timings are diagnostics.
See [R4c contracts and limits](archive/ecosystem-replacement-20260910/r4c-remaining-coverage.md).

## Loading Progress repaint pauses

`--loading-progress coalesce` enables the optional startup repaint improvement in candidate mode; default `off` leaves it disabled. The runtime command-line selector is `--wake-up-loading-progress=coalesce`. In the 0.2.1 baseline it validates the required repaint functions independently from the PNG bridge, preserves the native loading time budget and disables coalescing at the loaded menu. Missing or changed functions and interfering patches use ordinary loading. The frozen 0.14.0 identity records the original supplier evidence, not a whole-version admission gate. Progress can update less frequently within the existing budget. Inspect `repaint-coalescing.jsonl` for installation and a completed receipt with suppressed pauses and no refusals. This does not enable Gagarin XML reuse through Loading Progress; the separately tested XML bridge was not retained.

## Giddy-Up riding-offset preparation

`--giddy-textures on` enables optional center-column readback in candidate `startup-searches`; default `off` installs nothing. `timing` observes original readback and `verify` compares every original center-column alpha value. Offset calculation, setup order and settings writes remain intact. Match diagnostics between performance arms; verification replay is separate.

In the 0.2.1 baseline, four required texture/setup functions and compatible patch state determine admission. Windows Direct3D 11 and Linux OpenGL paths are implemented. Version 2.2.5.0 identifies the original supplier evidence; there is no whole-DLL version/hash admission restriction. Missing/changed suppliers, unsupported conditions, later calls and other threads retain ordinary behavior. Inspect `giddy-textures.jsonl` for actual hits, matching offset hashes, zero errors/mismatches and released textures. `offsetMs` measures complete offset calls; `scopeThroughMenuMs` includes later startup work. See [qualification and limits](archive/further-loading-improvements.md).

## Comparisons and stopping

Match content and order, selected features, supplier/application caches, observer and diagnostics. Use a reverse original control after a promising candidate to distinguish actual savings from Windows keeping recently read files in memory. Fresh application caches do not mean cold operating-system file caches. Do not evict global caches or create resource pressure merely to produce a win.

Show complete stage cost, including preparation, worker startup, joins and ordered completion where applicable. Nested or overlapping timers must not be added. Compare whole startup separately; stage savings may be too small or variable to improve the user-visible endpoint. Report exact runs and uncertainty instead of a universal percentage. Trust existing adequate semantics checks and add checks only for behavior actually changed. Stop a route when matched repeats erase its practical benefit.

Keep roadmap decisions, slice acceptance and evidence summaries in the [proposed steward workflow](ecosystem-replacement/next-campaign-proposal.md#11-steward-workflow-after-approval), and update current-state when the product changes. The [archived results](archive/results.md) preserve prior outcomes; do not use them as the active work queue. Raw logs, binary identities, private content and decompiled source remain ignored. Do not grow a new testing or documentation framework without a concrete product need.

## Publication

Use the [release procedure](release.md) for source-inclusive packaging, licensing, source-history handling and publication boundaries. The [current state](current-state.md) separates public 0.2.1, the older live-tested core, original rc.2 ZIP and corrected F1 fixture build. Older qualification statements belong to their exact packages. No new platform, gameplay or speed claim follows from a documentation change.

The proposed roadmap aims for one combined release after individual feature qualification. Preserve the owner's Workshop description and unrelated listing fields unless explicitly asked to change them. Build, deployment and publication are distinct actions. Live tests use the standing permission above or another explicit current grant; publication remains separate.

## Normal activation and post-startup qualification

For authorized qualification and matched performance runs, `--user-settings <JSON-file>` stages allowlisted ordinary settings in the isolated profile, records them in the run and verifies them before launch. It requires `--activation user` and a recorded functional or performance purpose. Example: `{"translationApplication":true,"loadingDisplay":true,"loadingDisplayDiagnostics":true}`. Product defaults are unchanged. `--language "French (Français)"` (or `English`) is also captured; English may have no useful translation-application lookups. Do not edit a prepared profile afterward.

The private observer runs feature probes only for automatic functional runs,
using the recorded `--fixture-functional-probes` argument. Performance arms
exclude them. Startup prepared-hit observations are separate from F1's bounded
post-menu checks. With prepared textures selected, the latter can drive one
small image through the real preparation controller without showing its window;
the reviewed carpet sample also exercises full/half/quarter DDS exports. The
controller check has a 40-second total budget before normal exit. With deferred
audio selected, supported first-use and bounded holder cleanup are checked;
holders retained by native audio sources are skipped. No probe enters a scene,
draws UI or tests audible quality. Maintenance launches skip preparation.
Inspect each feature receipt separately from `automaticTestPassed`; menu success
does not imply feature success. See [F1](archive/ecosystem-replacement-20260910/rc2-functional-qualification.md)
for exact scope and [the overnight report](archive/ecosystem-replacement-20260910/overnight-qualification.md)
for historical public-window evidence. PNG restoration admits its owned `v1`
layout with file identity verification.

`--prepared-cache-from <captured-label>` restores only the owned native entries and digest-named DDS exports/mappings after checking the captured normal exit/menu evidence, exact core/content selection and complete allowed-file hashes. A manually inspected preparation run may supply the store after its normal exit and capture. The next launch can change observer instrumentation and opt in through ordinary settings; runtime source/preset/selection checks still determine actual reuse. The target requires ordinary user activation and prepared textures enabled, or an explicitly functional bypass/rebuild/clear case. Saves and unrelated profile data are not restored by this option.

`prepare --mode candidate --activation user` includes the installed product and isolated save-data path but supplies no Wake-Up feature selectors. The mod reads its ordinary settings, including defaults when no file exists. This mode refuses simultaneous feature-selector overrides, preventing mislabeled comparisons. Explicit fixture controls continue to bypass normal settings.

The separate observer supports `--residual-probe` for bounded first-menu-call and post-cutoff type-search observation. This is a qualification arm, not an ordinary timing comparison. It adds no diagnostic code to the product package.

`--gameplay-smoke` explicitly selects a fixture-only small new-colony/tick/save/reload check after the independent menu timestamp. It also checks normal settings persistence when Wake-Up is present. It uses native game APIs, never UI automation or a normal-user save. The observer writes `FixtureMenuObserver/gameplay-smoke.json`, and launch records `gameplayTestPassed` separately from `automaticTestPassed`; both must pass. Failure is captured and rejected even when the process exits zero. This explicit mode allows ten minutes after menu readiness for gameplay and normal exit; routine startup comparisons retain the sixty-second limit and ordinary automatic exit. Do not count gameplay-smoke menu timings as scored speed comparisons. A nine-minute in-game timeout attempts normal shutdown and writes failure evidence.

Existing frozen seeds remain unchanged. Generated saves and settings belong only to the fixture capture, and the next ordinary preparation resets its profile from those seeds. These checks qualify a focused scenario; they do not establish arbitrary combat, long-running colony behavior or a public support matrix.

## Ordinary manual qualification

Use `--purpose functional --activation user --no-exit-after-menu-ready` for an
explicitly authorized screen-driven colony/load check. `--no-menu-observer`
excludes observer runtime entirely; retaining `--menu-observer` records only the
menu repaint and starts no gameplay/feature probe in manual mode. Neither manual
path changes native logging policy. Exit through Quit to OS and wait for capture;
manual runs have no `automaticTestPassed` claim. Record actual UI results separately.

`--gameplay-save-from <successful-automatic-label>` now also permits manual
functional loading. It restores only the existing verified fixture-created
`RloFixtureSmoke.rws`, preserving source/capture/save/content-order hash checks
at prepare and launch. It does not start an automatic load in manual mode.
Arbitrary or normal-user saves remain inadmissible. The
[ordinary-entry report](archive/ecosystem-replacement-20260910/ordinary-entry-final-candidate.md)
distinguishes manual passes from the earlier automated-probe failures.

## Isolated Steam version and three-arm comparisons

This is the later promotion procedure, used only after combined GOG acceptance and explicit owner approval for the Windows Steam release candidate. It is not the routine development loop.

The sole fixture can switch from its frozen GOG generation to the exact
reviewed Steam `1.6.4871 rev590` installation. `stage-steam --steam-source <path>`
copies and independently verifies the complete game except normal `Mods`.
Frozen fixture mods and their order remain in place. The existing transaction
journal preserves every replaced GOG game unit, profile, pointer and observer;
old snapshots and captures remain immutable. The new generation has its own
`runtimeContract`, game inventory and transition receipt. Inherited discovery
fields describe the frozen parent, not a refreshed mod collection. Never launch
recovery copies or treat these as additional fixtures.

`bind-comparison-package --package <path>` admits only the assigned exact public
0.2.1 or accepted retained package. It verifies original receipts and bytes,
copies the DLL unchanged, substitutes private fixture metadata and records both
original provenance and the current exact execution contract. This is admission
for testing, not a compatibility claim. Build references remain separately
recorded; GOG compilation does not pretend to be Steam compilation. Preparation
still refuses a mismatched candidate or observer, and the normal package guard
and operation lock remain in force. Build and deploy the Steam-targeted private
observer after switching; the old observer is preserved outside discovery.

Use `--mode absent` for the physically absent control and `--mode candidate
--activation user` for **both** public-old and new ordinary defaults. Fixture
`baseline` mode is not the public-old product. Keep native texture selection,
content order, language and unrelated settings identical. Separate warmups,
fresh application-cache starts and explicit cache restoration. Use balanced
forward/reverse repetitions and the independent menu-ready endpoint.

For a functional scene probe, `--native-stack-traces` leaves ordinary Unity
error/exception stack policy untouched and records that choice. It is refused
in performance runs. Without it, explicit gameplay/world probes retain the
previously documented private formatter workaround. A workaround-assisted save
pass does not qualify ordinary production scene entry. Timing-only launches
never select either gameplay probe or the private suppression.

## Save, world and new-colony qualification

Background loading is default off and admits supported ordinary saves, native
world-page generation and ordinary new-colony entry. Shared lifecycle guards
restore the user's current background preference and preserve native pause
behavior. Standalone maps and direct/autostart entry remain outside admission.
New-colony hooks were admitted in F1; actual entry/focus/pause behavior remains a separate authorized interactive check.

Existing `--world-background`, `--background-hold` and `--gameplay-save-from`
procedures and native receipt requirements are documented in the dated
[retained-feature qualification](archive/ecosystem-replacement-20260910/retained-features.md),
[scene/save report](archive/ecosystem-replacement-20260910/scene-save-qualification.md) and
[ordinary-entry report](archive/ecosystem-replacement-20260910/ordinary-entry-final-candidate.md).
Automatic scene probes used a private stack-trace workaround; later ordinary
screen-driven checks used native logging. Neither is proof that the shared
InputLegacy dependency error or earlier engine stall was repaired. These are
historical checks of the older core, not qualification of this candidate.

## Historical result retention — owner-authorized cleanup, 13 September 2026 UTC

Historical captures can contain repeated copies of identical large caches. The
owner authorized reclaiming unnecessary fixture data. The September cleanup
removes only verified duplicate cache payloads of at least 1 MiB from captures
before 10 September, retaining every unique payload in another historical result.
Original run manifests, logs, reports and current-campaign captures remain intact.
Completed rollback staging profiles are removed only when both the staging copy
and retained capture exactly match the original recorded inventory. Recovery
backups, fixture generations, frozen content and the active profile are preserved.

Completed cleanup removed 50,564,149,300 logical bytes (50.6 GB): 1,204 duplicate
historical cache files totaling 41.7 GB and 286 redundant staging profiles totaling
8.9 GB. The results folder fell from 74.5 GB to 32.8 GB in logical file size.
These are file-byte totals, not a measurement of disk allocation or game performance.

The private retention index is
`artifacts/ecosystem-next-20260910/fixture-cleanup-deleted.jsonl`; each entry records
the removed path, original timestamp, byte count, SHA-256 and surviving identical
payload. Its retained paths are now archival dependencies: preserve them and this
index during future cleanup. A missing indexed historical copy is intentional
compaction, not a new failed test or evidence of product corruption. No unique
payload was discarded. Final totals are in `fixture-cleanup-summary.json` beside it.

Before passing a compacted old result to a fixture cache-import operation or
rerunning its full inventory audit, restore its removed copies under the usual
exclusive fixture ownership:

```powershell
python artifacts/ecosystem-next-20260910/fixture-restore-compacted-result.py RUN_LABEL
```

This private helper uses the fixture lock and process guard, checks retained
content and restores original timestamps. It does not launch the game or alter
run manifests. Ordinary current-campaign cache imports need no restoration.
