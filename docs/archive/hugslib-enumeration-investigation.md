> Historical snapshot, preserved during the 2026-09-06 cleanup. This is evidence, not current operating instructions or a claim about the cleaned product. See [current state](../current-state.md) and [results](../results.md).
> Original: `records/implementation/OP7_OVERNIGHT_HUGSLIB.md` at `f886277ace5ed9b71fb044e69b57cb2cee5269f3`. Former branches, runtime files, commands and local paths below may no longer exist. Unretained local links are written as historical paths; recover their originals through the [history index](README.md).

# Optional HugsLib obsolete-report filtering

Historical branch handoff: deployed runtime and fixture selection below describe
this experiment, not the current fixture. See [the later restored state](loading-experiments-closeout.md).

**Parked after owner scope correction, 2026-09-06.** The owner wants loading
improvements that generalize across modlists, allowing supplier compatibility
when it supports the loading pipeline itself. This supplier-specific warning
optimization is not being promoted or further qualified. It is parked for
scope, not rejected on performance. Main remains `7d464ea`.

Selective integration was performed on `codex/overnight-hugslib-integration`
directly from main `7d464ea`. The owner authorized overnight investigation,
implementation and all fixture workload sizes while the PC is idle. This
authority does not change normal Steam/profile/save boundaries or publish a release.

The change skips expensive Harmony patch-record reads for methods that cannot
appear in HugsLib's obsolete-method warning. It retains HugsLib's lazy ordered
pipeline, active-patch checks, owner grouping and original warning output.
The exact known Performance Optimizer prefix still removes its internal
transpiler from the temporary report object for retained obsolete methods.
No persistent cache or worker is added.

Only the HugsLib runtime/tests from research `54bee10`, one initializer call,
the small shared patch-snapshot helper overload and necessary fixture selector
support were retrieved. The existing Character Editor helper caller supplies
no admission callback and retains strict foreign-hook rejection. Existing
searches, Gagarin and DDS implementations are otherwise unchanged. HugsLib and
Performance Optimizer are optional and are not assembly references.

Use `--hugslib-filter on` with candidate `startup-searches`; default remains
`off`. `timing` measures the original report for comparisons. The `hugslib`
lane selects the ordinary official/Prepatcher/Harmony foundation plus the two
frozen suppliers in original relative order. Unknown supplier identities or
report-hook chains refuse locally, leaving original behavior and other features.

Prior research active full runs avoided about 4,100 patch-record reads and
1.5 CPU seconds within the report, preserving actual warnings and captured XML.
That stage benefit does not establish a substantial whole-startup improvement.
Integrated-package qualification began with supplier absence and small
activation. The scope correction arrived during the full original control.
That healthy launch was allowed to exit and capture normally; the full filter
arm was never launched. No integrated full-profile gain or live warning-output
equality comparison is established.

Initial verification: nine focused HugsLib checks pass, including actual pinned
report equality/order and Performance Optimizer local mutation. Thirty-eight
existing feature checks and all 25 fixture Python checks pass, with no skips.
One new Python assertion initially
matched the evidence directory's name instead of the argument prefix; corrected
to check the exact selector. No product failure was involved.

## Captured outcomes and final state

Runtime `d0ee269d7369b35c9f01a6ed603c4ca010cf3d5f` was built with zero warnings
or errors and explicitly deployed with `deploy --package`. Candidate metadata,
each prepared run and capture bind that exact revision. The unchanged observer
and normal automatic exit ran in all three captures. Every run exited zero with
`automaticTestPassed=true`; the game and launcher are closed.

| Label | Behavior | Menu seconds | HugsLib report ms / thread CPU ms |
| --- | --- | ---: | ---: |
| night-hugs-a01-absence | Optional suppliers absent; searches still run | 10.043940 | No report |
| night-hugs-s01-on | Filter active with exact Performance Optimizer support | 10.665271 | 5.065 / 0.000 |
| night-hugs-f01-original | Full original report, filter disabled | 191.812735 | 3993.072 / 953.125 |

The small active run examined 94 methods, avoided 94 patch-record reads,
retained zero obsolete methods and reported no exception. The coarse Windows
thread-CPU counter's zero for this short scope is not zero work. The full
control preserves the actual TrainsOfTheRim warning concerning
`Vehicles.World.EnterMapUtilityVehicles.SpawnVehicles`; no full candidate exists
to compare it with. The report scope surrounds admission and original report
execution, but excludes its subsequent receipt-file write. No worker setup,
join or ordered preparation is introduced. Startup installation is included in
menu observation. The 191.813-second menu is a control observation only.

All three runs request accepted startup-searches and optional Gagarin reuse;
the full control uses freshly prepared application caches, so normal Gagarin
miss/cache creation still occurs. Original main DDS is retained. The final
selected label is `night-hugs-f01-original`, HugsLib `timing` and Gagarin `on`.
HugsLib defaults off; neither supplier is a mandatory dependency. Runtime
`d0ee269` remains deployed; later closeout changes are documentation only.

Evidence is private under `artifacts/overnight-hugslib`: `build.log`,
`deploy.log`, `hugslib-checks.log`, `existing-feature-checks.log`, per-run
preparation/launch logs and `captured-summary.json`. Original receipts and
Player.log are in the corresponding canonical fixture result directories.
No normal Steam/Workshop/profile/save/cache state was changed; no UI automation,
security changes, forced closures, merge, push or publication occurred.

The DDS overnight result is retained separately in
[OP7_OVERNIGHT_DDS.md](dds-read-ahead-qualification.md): its newer reader showed no useful
idle warm startup gain and remains research-only. No DDS runtime was imported
here. PNG remains research-only and has no established medium/full speedup.
OP7 remains incomplete and OP8 inactive.
